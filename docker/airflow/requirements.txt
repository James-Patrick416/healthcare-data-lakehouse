# -------------------------------------------------------------------
# Healthcare Data Lakehouse
# Python dependencies installed into the custom Airflow image.
#
# Pinning versions improves reproducibility across environments and
# prevents unexpected breaking changes during future builds.
# -------------------------------------------------------------------

# Data manipulation
pandas==2.3.1
pyarrow==21.0.0

# Embedded analytical database
duckdb==1.3.2
duckdb-engine==0.17.0

# S3 / MinIO interaction
boto3==1.40.0
minio==7.2.16

# Data validation
great-expectations==1.5.11

# Environment variables
python-dotenv==1.1.1

# dbt adapter for DuckDB
dbt-duckdb==1.9.4